// Arrayer, eller vektorer, är variabler med flera värden.

//            0               1              2
let breeds = ['Russian Blue', 'Arabian Mau', 'Siberian']

// .length ger oss antalet värden i en array
console.log('There are', breeds.length, 'breeds in the list')

// när man vill komma åt ett värde anger man dess position i arrayen, d.v.s. dess index
breeds[0] // första ligger på index 0
breeds[1] // andra ligger på index 1
breeds[2] // tredje ligger på index 2

// for (let x = 0; x < breeds.length; x++) {
//   console.log(breeds[x])
// }
